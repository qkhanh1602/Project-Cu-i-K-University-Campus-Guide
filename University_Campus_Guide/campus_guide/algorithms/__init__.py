"""Pure, easy-to-read algorithm references.

The UI uses campus_guide.algorithmsUI because those files also build trace data
for animation. This package keeps the same ideas without TraceStep/UI code, so
it is easier to read during an oral exam.
"""
